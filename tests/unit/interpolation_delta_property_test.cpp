// instrument scaffold -- implemented in the wave-2 plan

#include <catch2/catch_test_macros.hpp>

TEST_CASE("interpolation_delta_property instrument scaffold compiles",
          "[interpolation_delta_property]")
{
    REQUIRE(true);
}
